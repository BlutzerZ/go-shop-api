package routes

import (
	"github.com/gin-gonic/gin"
)

func OrderRoutes(r *gin.Engine) {
	// r.GET("/order", controllers.GetOrders)
	// r.GET("/order/:orderID", controllers.GetOrderByID)
	// r.PUT("/order/:orderID/cancel", controllers.CancelOrderByID)
}
